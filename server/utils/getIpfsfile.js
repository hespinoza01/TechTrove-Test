// const helia = require('@helia/verified-fetch')

module.exports = async uri => {
    try {
        console.log(helia)
        // const response = await verifiedFetch(uri)

        // console.log(response)
    } catch (error) {
        console.log(error)
        return null
    }
}
